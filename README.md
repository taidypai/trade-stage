trade-stage/
  
     ├── backend/ # Аналитическое ядро и интеграция с QUIK

│ ├── components/ # Вспомогательные компоненты
│ │ ├── analis_candle/ # Анализ свечных паттернов
│ │ │ ├── analis.py
│ │ │ ├── config_init.py
│ │ │ └── del.py
│ │ │
│ │ ├── quik_components/ # Компоненты для работы с QUIK
│ │ │ ├── quik_account.py # Управление счетами
│ │ │ ├── quik_ticker.py # Получение данных по тикерам
│ │ │ └── quik_transaction.py # Отправка транзакций
│ │ │
│ │ ├── logger.py # Система логирования
│ │ ├── start_quik.py # Инициализация подключения к QUIK
│ │ ├── tg_message.py # Отправка уведомлений в Telegram
│ │ └── time_service.py # Сервис времени и торговых сессий
│ │
│ ├── QuikPy/ # Обертка для взаимодействия с QUIK
│ │ ├── init.py
│ │ ├── QuikPy.py
│ │ ├── README.md
│ │ └── setup.py
│ │
│ └── services/ # Аналитические сервисы
│ ├── get_price_service/ # Получение текущих цен
│ │ ├── init.py
│ │ └── detector_price.py
│ │
│ ├── impuls_service/ # Детектор ценовых импульсов
│ │ ├── init.py
│ │ └── detector_impuls.py
│ │
│ ├── liquidity_service/ # Детектор зон ликвидности
│ │ ├── init.py
│ │ └── detector_liquid.py
│ │
│ ├── order_block_service/ # Детектор ордер-блоков
│ │ ├── init.py
│ │ └── detector_orb.py
│ │
│ ├── run_service/ # Оркестратор сервисов
│ │ ├── init.py
│ │ └── run_all.py
│ │
│ └── trading_engine/ # Торговый движок
│ └── init.py
│
├── docs/ # Документация и Lua-скрипты
│ ├── QuikPy/ # Документация QuikPy
│ │ ├── Examples/
│ │ ├── QUIK/
│ │ │ └── lua/ # Lua-скрипты для QUIK
│ │ │ ├── config.json
│ │ │ ├── dkjson.lua
│ │ │ ├── qscallbacks.lua
│ │ │ ├── qsfunctions.lua
│ │ │ ├── qsutils.lua
│ │ │ ├── Quik_2.lua
│ │ │ ├── QuikSharp.lua
│ │ │ ├── socket.lua
│ │ │ └── socket/
│ │ │ └── core.dll
│ │ ├── init.py
│ │ ├── QuikPy.py
│ │ ├── README.md
│ │ └── setup.py
│ ├── README.md
│ └── Структура.txt
│
├── frontend/ # Telegram бот (пользовательский интерфейс)
│ └── helper_pro/
│ ├── handlers/ # Обработчики команд бота
│ │ ├── callback_routers.py # Обработка инлайн-кнопок
│ │ └── start_router.py # Обработка команды /start
│ │
│ ├── helper_start.py # Точка входа в бота
│ ├── keyboards.py # Клавиатуры для бота
│ └── config_bot.py # Конфигурация бота
│
├── settings/ # Конфигурационные файлы и данные
│ ├── logs/ # Логи работы системы
│ │ ├── errors.txt
│ │ └── info.txt
│ │
│ ├── backend_config.py # Конфигурация backend
│ ├── frontend_config.py # Конфигурация frontend
│ └── market_data.json # Рыночные данные
│
└── README.md # Этот файл
### Принцип работы (Связь Backend и Frontend)

Система использует файловый обмен данными для связи независимых частей:

1.  **Frontend (Telegram Bot):** Получает команды от пользователя и изменяет параметры в файле `settings/CONFIG_PROCESS.json`.
2.  **Backend (Analytics Core):** Постоянно мониторит изменения в `CONFIG_PROCESS.json`. Обнаружив новую задачу (например, запрос данных по конкретному тикеру), запускает соответствующий сервис анализа, получает данные из QUIK через прослойку `QuikPy`, обрабатывает их и сохраняет результаты в JSON-файлы (`market_data.json`, `CRATE_PRICE.json`, `GET_LEVEL.json` и др.).
3.  **Frontend (Telegram Bot):** Получает уведомление от Backend (через изменение статуса `status_data`) о готовности данных и отображает их пользователю.

---

## ⚙️ Компоненты Backend

Backend (`/backend`) — ядро системы, отвечающее за всю аналитику и взаимодействие с терминалом QUIK.

### 1. Ядро и интеграция с QUIK
*   **`QuikPy/`**: Обертка для взаимодействия с QUIK через Lua-скрипты. Позволяет получать реальные рыночные данные и отправлять транзакции.
*   **`components/start_quik.py`**: Модуль для инициализации подключения к QUIK через QuikPy.
*   **`components/quik_components/`**:
    *   `quik_account.py`: Управление счетами.
    *   `quik_ticker.py`: Получение данных по тикерам.
    *   `quik_transaction.py`: Отправка транзакций (заявок) в стакан.
*   **`components/logger.py`**: Модуль логирования для отслеживания работы системы.
*   **`components/time_service.py`**: Сервис для работы с временными метками и торговыми сессиями.
*   **`components/tg_message.py`**: Модуль для отправки уведомлений в Telegram.

### 2. Аналитические сервисы (`/backend/services/`)
Каждый сервис отвечает за детектирование определенной рыночной ситуации.

*   **`get_price_service/`**: Сервис получения текущих цен.
*   **`impuls_service/`**: Детектор ценовых импульсов.
*   **`liquidity_service/`**: Детектор зон ликвидности.
*   **`order_block_service/`**: Детектор ордер-блоков.
*   **`run_service/run_all.py`**: Главный оркестратор, запускающий все сервисы анализа.
*   **`trading_engine/`**: Модуль для принятия торговых решений на основе данных от детекторов.

### 3. Вспомогательные компоненты (`/backend/components/`)
*   **`analis_candle/`**: Модуль для анализа отдельно взятой свечи.

---

## 💬 Компоненты Frontend

Frontend (`/frontend`) реализован в виде Telegram-бота.

*   **`helper_start.py`**: Точка входа в бота.
*   **`handlers/`**:
    *   `start_router.py`: Обработчик команды `/start` и начального меню.
    *   `callback_routers.py`: Обработчик нажатий на инлайн-кнопки.
*   **`keyboards.py`**: Файл с шаблонами клавиатур (Reply и Inline).
*   **`config_bot.py`**: Конфигурация бота (токен, администраторы).

---

## ⚙️ Конфигурация и файлы данных (`/settings`)

Этот каталог является центральным звеном для настройки системы и обмена данными.

### 1. Основные конфигурационные файлы
*   **`backend_config.py`** и **`frontend_config.py**`: Python-модули для чтения настроек.
*   **`CONFIG.json`**: Статические настройки системы.
    *   `quik_path`, `quik_password`: Данные для подключения к QUIK.
    *   `finam_api_token`, `finam_api_account_id`: Данные для альтернативного подключения к Finam (если используется).
    *   `TRADING_TIKERS`: Список доступных для торговли тикеров.
    *   `TIMEFRAMES`: Поддерживаемые таймфреймы.
*   **`CONFIG_PROCESS.json`**: **Файл для динамического управления процессами.** Это основной канал связи Frontend -> Backend.
    *   `start_settings`: Параметры запуска и статусы.
        *   `status_backend`: (Обновляется Backend) `true` - бэкенд работает.
        *   `status_day`: (Обновляется Backend) `true` - сегодня торговый день.
    *   `trade_settings`: Торговые настройки.
        *   `get_data`: (Устанавливается Frontend) Тикер для анализа (например, `VTB-12.25`).
        *   `status_data`: (Обновляется Backend) `true` - данные по запрошенному тикеру готовы.

### 2. Файлы данных (создаются/обновляются автоматически)
*   **`market_data.json`**: Общие рыночные данные.
*   **`CRATE_PRICE.json`**: Хранилище текущих цен по разным биржам (`FINAM`, `OKX`, `FOREX`).
*   **`GET_LEVEL.json`**: Хранилище текущих уровней поддержки/сопротивления.
*   **`GET_GAP.json`**: Хранилище данных по GAP-ам для выбранного пользователем тикера.
*   **`1h.json`, `4h.json`, `1d.json`**: Исторические данные свечей для соответствующих таймфреймов.
    *   `candles`: Список свечей, где каждая свеча содержит `candle_id`, `open`, `close`, `heigh`, `low`.

### 3. Логи
*   **`logs/errors.txt`**: Файл с записями об ошибках.
*   **`logs/info.txt`**: Файл с информационными сообщениями о работе системы.

---

## 📚 Документация и Lua-скрипты (`/docs`)

*   **`QuikPy/`**: Полная копия документации и исходного кода библиотеки QuikPy.
*   **`QuikPy/QUIK/lua/`**: Набор Lua-скриптов, которые необходимо поместить в папку `Scripts` терминала QUIK.
    *   `QuikSharp.lua`: Основной скрипт-мост для взаимодействия с Python.
    *   `config.json`: Конфигурация для Lua-скриптов.
    *   Вспомогательные библиотеки (`dkjson.lua`, `socket.lua` и др.).

---

## 🚀 Запуск и установка

### Предварительные требования
1.  Установленный терминал **QUIK**.
2.  **Python 3.8+**.
3.  Установленные зависимости: `pip install -r requirements.txt` (файл `requirements.txt` нужно создать отдельно).
4.  Telegram Bot Token (получить у @BotFather).

### Настройка
1.  **Настройка QUIK:**
    *   Скопируйте все файлы из `docs/QuikPy/QUIK/lua/` в папку `Scripts` вашего терминала QUIK.
    *   Отредактируйте `Scripts/config.json`, указав нужные параметры (порты при необходимости).
    *   Запустите QUIK и подключите скрипт `QuikSharp.lua` (Через меню "Сервисы" -> "Lua скрипты" -> "Добавить").
2.  **Настройка приложения:**
    *   Отредактируйте файл `settings/CONFIG.json`, указав ваши данные (путь к QUIK, пароль, счет, список тикеров).
    *   В файле `frontend/config_bot.py` укажите токен вашего Telegram-бота.
3.  **Запуск:**
    *   Запустите Backend-часть: `python backend/start.py` (или через `run_all.py`).
    *   Запустите Frontend-часть: `python frontend/helper_start.py`.

После запуска бот будет готов принимать команды. Выберите тикер в меню, и система начнет анализ.

---

## 🛠️ Технологический стек

*   **Язык:** Python 3.10+
*   **Терминал:** QUIK (ARQA Technologies)
*   **Интеграция с QUIK:** [QuikPy](https://github.com/RuslanMishin/QuikPy) (адаптированная версия)
*   **Фронтенд:** Aiogram 3.x
*   **Обмен данными:** JSON-файлы
*   **Логирование:** Logging
*   **Работа с данными:** Встроенные библиотеки Python

---

## ⚠️ Важные замечания

*   Система предназначена для работы на реальных рынках. Используйте на свой страх и риск.
*   Тщательно тестируйте новых стратегий на демо-счете перед запуском на реальные деньги.
*   Убедитесь, что терминал QUIK работает стабильно и имеет непрерывное соединение с сервером брокера.

---
